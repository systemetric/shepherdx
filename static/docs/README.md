# Docs

Docs should be generated from the submodule and placed in this directory.

The `build-shepherd` build command can be used from the submodule, which
will automatically place files here, this only works on *nix however.
