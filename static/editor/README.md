# Sheep

Build Sheep from the submodule and place artifacts in this directory.

Use the `build-shepherd` build command from within the submodule to
automatically place generated files here, this only works on *nix.
